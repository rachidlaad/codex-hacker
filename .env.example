# Security backend endpoint used by Codex CLI.
BACKEND_BASE_URL=http://127.0.0.1:8000/v1

# Bearer token used to authenticate requests to your backend.
BACKEND_API_TOKEN=replace-with-your-token

# Optional in-scope target URL/domain for auth/session testing.
BACKEND_SECURITY_TARGET=https://target.example.com
